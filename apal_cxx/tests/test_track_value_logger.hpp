#ifndef TEST_TRACK_VALUES_LOGGER_H
#define TEST_TRACK_VALUES_LOGGER_H

/** Check if the keys are correctly read from file */
bool read_from_file();

/** Test initialise keys from entry */
void init_keys_from_entry();

/** Try to initialise the file from an existing file */
void track_values_append();
#endif